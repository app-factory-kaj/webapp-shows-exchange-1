{
  "name": "exchange-api",
  "type": "service",
  "version": "0.1.0",
  "language": "Ballerina",
  "buildpack": "docker",
  "appPath": "exchange-api",
  "entrypoint": "deployment/service",
  "exposure": "intranet",
  "dependencies": [
    { "kind": "platform-resource", "name": "thunder-app", "resourceType": "thunder-app", "parameters": { "scopes": "openid profile email" }, "description": "Validates the bearer token on incoming requests (via the gateway) to identify the signed-in caller." },
    { "kind": "external", "name": "exchange-rate-provider", "style": "rest-api", "description": "ExchangeRate-API (https://v6.exchangerate-api.com) — fetches daily USD-based exchange rates via GET /v6/{API_KEY}/latest/USD; the service picks the requested currency's rate out of the returned rates map. Requires an API key; requires a specPath before the build gate resolves it.", "config": [ { "key": "EXCHANGE_RATE_API_KEY", "secret": true, "description": "API key for ExchangeRate-API (v6.exchangerate-api.com), used to authenticate the /v6/{API_KEY}/latest/USD request." } ] }
  ],
  "description": "Ballerina REST service that lists supported currencies and returns the current exchange rate of a given currency to USD, sourced daily from an external exchange-rate provider. Sits behind the gateway (bearer-token protected) and is called only by exchange-webapp. Does not perform arbitrary amount conversion, history, or store per-user data.",
  "skillsPinned": ["openapi-conventions", "ballerina", "api-management", "thunder-authentication"]
}
