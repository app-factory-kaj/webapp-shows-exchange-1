title Exchange Rate Lookup

phase 1

component exchange-webapp as "Exchange Rate Webapp" web-application [stories: 1, 2, 3, 4]
component exchange-api as "Exchange Rate API" service [stories: 2, 3, 4]

south exchange-rate-provider as "ExchangeRate-API" service

north -> exchange-webapp

exchange-webapp -> exchange-api
exchange-api -> exchange-rate-provider
